# Service OCR Bobines — dépendances de PRODUCTION (légères).
# L'extraction se fait par IA visuelle (NVIDIA NIM) : aucune dépendance
# lourde nécessaire → déploiement rapide sur Render / conteneur léger.
#
# Le repli OCR local (PaddleOCR) est OPTIONNEL → voir requirements-ocr.txt.

# ── API Web ───────────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ── Traitement image ──────────────────────────────────────────────────────────
Pillow>=10.0.0

# ── Base de données (PostgreSQL local + Supabase prod) ────────────────────────
psycopg2-binary>=2.9.9

# ── Config & Validation ───────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
